# Парсим натуральное число.
ParseInt ← +⟜(×⟜10)´ ∘ ⌽ ∘ -⟜'0'

# Функция для вычисления "количества топлива" по "весу модуля".
GetFuel ← -⟜2 ∘ ⌊ ∘ ÷⟜3

# Первая часть - считаем для каждого модуля и суммируем.
Part1 ← +´ GetFuel¨

# Функция для вычисления "общего количество топлива" по "весу модуля".

# - Последовательно применяем функцию GetFuel, пока результат не станет ≤0.
# - Складываем все значения в массив.
# - Суммируем.

# Глиф ⍟ повторяет левую часть столько раз, сколько указано в правой части. В правой части
# будет либо 0, либо 1 - в зависимости от того, какой первый элемент массива. В левой части
# стоит трейн, который добавляет в начало массива результат функции GetFuel. После чего
# вызывается функция 𝕊, т.е. рекурсивно вызывается self, функция, определенная в блоке {}.
# Таким образом мы добавляем новые элементы в массив, пока не получим значение ≤0.

GetTotalFuel ← +´ 1↓  {𝕊∘(GetFuel∘⊑ ∾ ⊢) ⍟ (>⟜0 ⊑) 𝕩} ∘ GetFuel

# Вторая часть - считаем для каждого модуля и суммируем.
Part2 ← +´ GetTotalFuel¨

data ←  ParseInt¨ •FLines "aoc19_01.txt"

•Show Part1 data
•Show Part2 data
